<!-- Generated by Agent Atlas. Do not edit directly. -->

# Atlas overview

Profile: `public`

## What this atlas represents

- `domain:reclaim-toolkit-local` - reclaim-toolkit Local Surface: Repo-local architecture, docs, and verification surfaces for reclaim-toolkit.

## Start here

- Begin with a domain to understand the broad area.
- Drill into workflows to see capabilities and processes.
- Follow components, documents, and tests only as needed for the task.
- Use path resolution for bottom-up navigation from a source file.

## Major capabilities

### reclaim-toolkit Local Surface

- No workflows are directly attached to this domain.

## Implementation surfaces

### reclaim-toolkit Local Surface

- `component:reclaim-toolkit-source` - reclaim-toolkit Source Surface: Main source, configuration, scripts, docs, and tests agents should inspect before broad repository search.

## Verification

- No test scopes are attached to the overview.

## How to drill down

- `atlas show <entity-id>` shows one entity and its immediate relations.
- `atlas neighbors <entity-id> --depth 2` traverses nearby graph context.
- `atlas resolve-path <path>` maps source files to owning context.
- `atlas context-pack "<task>" --budget 4000` selects task-specific reads and verification.

## Other entities

- `document:reclaim-toolkit-readme` (document) - reclaim-toolkit README: Primary repo entrypoint for setup, usage, architecture, and maintenance notes.
- `repository:reclaim-toolkit` (repository) - reclaim-toolkit: TypeScript utilities and an npm-first CLI for working with Reclaim.ai APIs.
- `test-scope:reclaim-toolkit-verification` (test-scope) - reclaim-toolkit Verification: Narrow default verification commands for repo-local changes; prefer more specific tests when the task identifies them.
